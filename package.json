{
  "name": "loot-market",
  "private": true,
  "type": "module",
  "scripts": {
    "build:assets": "node scripts/build-assets.mjs",
    "check:worker": "npm run build:assets && wrangler deploy --dry-run"
  },
  "dependencies": {
    "better-auth": "1.7.2"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
